name: Email Action Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f1000184@ds.study.iitm.ac.in
        run: echo "Hello, GitHub Actions!"
